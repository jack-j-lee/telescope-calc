# Telescope Calc – cloud deployment
# Copy to .env and set values. Used by docker-compose and platform configs.

# Backend: comma-separated origins allowed by CORS (e.g. https://app.example.com)
# Use * when behind same-host reverse proxy (e.g. docker-compose.cloud).
CORS_ORIGINS=*

# Frontend: API base URL. Empty or / for same-origin (reverse proxy).
# Set to backend URL when frontend and API are on different hosts.
NEXT_PUBLIC_API_BASE=
